<Window x:Class="NetworkService.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetworkService"
        xmlns:viewModel="clr-namespace:NetworkService.ViewModel"
        xmlns:views="clr-namespace:NetworkService.Views"
        xmlns:data="clr-namespace:NetworkService.Model"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="480"
        FontSize="17">

    <Window.DataContext>
        <viewModel:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:NetworkEntitiesViewModel}">
            <views:NetworkEntitiesUserControl/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:NetworkDisplayViewModel}">
            <views:NetworkDisplay/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:MenuViewModel}">
            <views:MenuControl/>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Label Content="MENU:" FontSize="15" VerticalAlignment="Center"/>
                <ComboBox Width="60" Height="40" Margin="0,0,25,0"/>
                <Button Width="60" Content="HOME" Height="40" Margin="25,0,25,0"/>
                <Button Width="60" Content="UNDO" Height="40" Margin="25,0,25,0"/>
            </StackPanel>
            
        </Grid>
        
    </Grid>
</Window>
